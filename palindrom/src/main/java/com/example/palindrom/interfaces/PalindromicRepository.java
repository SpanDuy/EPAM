/*package com.example.palindrom.interfaces;

import com.example.palindrom.word.Palindromic;
import org.springframework.data.jpa.repository.JpaRepository;

import java.util.Optional;

public interface PalindromicRepository extends JpaRepository<Palindromic, String> {
    Optional<Palindromic> findByWord(String word);
}
*/